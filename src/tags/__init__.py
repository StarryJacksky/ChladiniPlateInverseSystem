# TAGS target-aligned passive geometry helpers. / TAGS 目标对齐被动几何工具。

from src.tags.target_aligned_geometry import TagsCase  # 导出 TAGS 案例配置 / Export TAGS case config
from src.tags.target_aligned_geometry import build_default_tags_cases  # 导出默认案例构造 / Export default case builder
from src.tags.target_aligned_geometry import build_tags_bands  # 导出目标带构造 / Export target-band builder
from src.tags.target_aligned_geometry import generate_tags_thickness_matrix  # 导出厚度矩阵生成 / Export thickness generator
