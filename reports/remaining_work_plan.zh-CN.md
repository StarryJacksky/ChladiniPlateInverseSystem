# 剩余工作顺序表

[语言](./remaining_work_plan.md): [双语](./remaining_work_plan.md) | 中文 | [English](./remaining_work_plan.en.md)

> 算法主线规划见 [`algorithm_breakthrough_plan.zh-CN.md`](./algorithm_breakthrough_plan.zh-CN.md)。本文聚焦"维护性 / 等待外部输入"两类任务，不再包含算法突破工作流。

本文把 Codex 现在还能继续做的任务，与需要真实材料、COMSOL 或实体测试数据后才能做的任务分开。

## A. 现在可以独立继续做

当前可独立推进的队列已经主要是质量维护，而不是继续堆新功能。

### 1. 质量门维护

任务：

1. 每次有实质代码或文档改动后，运行 `python scripts/check_local_quality.py`。
2. 保持手写 `.py`、`.m`、`.html` 文件通过中英双语注释检查。
3. 只有出现新的可重复本地检查时，才扩展本地质量检查合集。

已完成基础：

- 质量检查合集现在覆盖文档链接、双语注释、配置合同、自动发现样例、产物清理、无 COMSOL 烟测、Python 编译和可选前端语法检查。

### 2. 文档维护

任务：

1. 保持根目录 README 是简洁双语入口页，并链接到用户文档。
2. 后续实现变化时，同步维护部署指南、软件说明书、代码结构说明和规划报告。
3. 只有在 UI 和 COMSOL 工作流稳定后，再补最终截图和实验室专属说明。

已完成基础：

- 面向零基础用户的双语文档已经覆盖安装、软件使用、项目结构、本地检查、运行报告和剩余工作。

### 3. 条件允许时的视觉验证

任务：

1. 当本地 server 权限或其他可用预览方式恢复后，重新用浏览器打开本地应用。
2. 检查 Target、Tune、Results、Run 的桌面和窄屏布局。
3. 等界面不再快速变化后，再为 README 和软件说明书截最终图。

当前说明：

- 最近一次本地预览被 server/file 预览权限挡住，所以这一项等待可用预览路径，而不是继续写更多功能代码。

## B. 需要真实项目数据后再做

### 1. 材料校准

需要实测或可信的密度、杨氏模量、泊松比和热参数。

### 2. COMSOL 基准模型冻结

需要最终 MPH 模型、最终孔/垫片假设、最终边界命名和最终固定约束。

### 3. 实体验证

需要实体板频率、激励设置，以及拍摄或提取的实体节点线图案。

### 4. 优化调参

需要足够 COMSOL 结果来调整评分权重、随机搜索设置和遗传算法算子。

### 5. 干净机器验证

需要真实 Windows、macOS、Linux 机器，或等价干净虚拟机，并安装实际 COMSOL/MATLAB 来确认发现逻辑。

### 6. 最终发布指南

需要稳定 UI 截图、最终安装路径、实验室专属 COMSOL/MATLAB 注意事项和确认过的故障排查案例。

## C. 当前等待边界

Codex 现在已经接近计划中的等待边界。

现在大多数有意义的新工作需要以下输入之一：验收后的最终 MPH 模型、真实材料参数、真实 COMSOL 输出图片/CSV、实体测量数据、干净系统验证机器，或可用的本地浏览器预览路径。

在这些输入到位前，安全的独立工作主要是维护：同步文档、保持检查通过，并避免编造会和真实 COMSOL 基准模型冲突的模型专属假设。
